all:
  children:
    helper:
      hosts:
        helper-node:
          ansible_host: 192.168.122.2
          ansible_user: ansible
        # ansible_password: xxxxx #or add passwordless sudo rights to the ansible user https://developers.redhat.com/blog/2018/08/15/how-to-enable-sudo-on-rhel
        # Remember to use ssh-copy-id to make sure your host can log into the helper
    windows:
      hosts:
        windows-db-node:
          ansible_host: cmf-db-win2k19.cmf-ansible-environment.svc.cluster.local
          ansible_user: Administrator
          ansible_password: "super-secret-password"
          ansible_connection: winrm
          ansible_winrm_transport: basic
          #some additional lines for when using non-standard ports
          # ansible_winrm_port: 30985
          # ansible_winrm_server_cert_validation: 'ignore'
          # ansible_winrm_scheme: 'http'

